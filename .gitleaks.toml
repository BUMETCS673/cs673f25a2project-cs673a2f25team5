title = "Gitleaks"

[allowlist]
paths = [
  "docs/",
  "code/backend/.tox/",
  "code/backend/.ruff_cache/",
  ".venv/",
]
regexes = [
  '''(?i)example(api|secret|token)_?key''',
  '''(?i)dummy(-|_)token''',
]
